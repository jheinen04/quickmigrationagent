# ADR-003: Data Architecture

**Status:** Pending
**Export Date:** 2026-09-08T13:54:28.765Z

---

## Original ADR Document

ADR-003: Data Architecture — SPICE Capacity, Direct Query, and Data SourcesStatusProposedDate2026-09-08Decision MakersData ArchitectEnterprise ArchitectPlatform Engineering LeadContext and Problem StatementQuickSight offers two data access modes: SPICE (Super-fast, Parallel, In-memory Calculation Engine), which caches data as refreshable snapshots, and direct query, which sends queries to the underlying data source on every request. This decision has cross-cutting implications for performance, cost, data freshness, and source system load. Most organizations adopt a hybrid approach.Decision DriversData freshness requirements per use case (real-time vs. near-real-time vs. batch)Dataset sizes and growth projectionsSource system performance and cost characteristics (per-query pricing like Athena)VPC connectivity requirements for private data sourcesRegion selection and data residency requirementsSPICE capacity budgetConsidered OptionsSPICE-first — Import all datasets into SPICE with scheduled refreshesDirect Query-first — Query source systems directly for all dashboardsHybrid approach — SPICE for high-volume dashboards, direct query for real-time operational viewsDecision Outcome[To be decided]Consequences of Each OptionOption 1: SPICE-firstGood: Best query performance via columnar storage and in-memory technologiesGood: Reduces per-query costs for sources like Athena (charges only at creation/refresh)Good: Data reusable multiple times without additional costsBad: Data staleness bounded by refresh frequency (max 32 on-demand full refreshes per 24h)Bad: Requires SPICE capacity planning and budget ($0.38/GB/month in Enterprise)Bad: Max dataset size: 1B rows / 1 TB (Enterprise)Option 2: Direct Query-firstGood: Real-time data freshness — always currentGood: No SPICE capacity planning neededBad: Performance depends entirely on source databaseBad: Higher costs for per-query-priced sources (Athena, etc.)Bad: Source system load increases with dashboard usageOption 3: Hybrid approach (Recommended)Good: Optimizes cost and performance per use caseGood: Real-time where needed, cached where acceptableNeutral: Requires per-dataset decision documentationBad: More complex to manage and monitorSPICE Capacity PlanningParameterStandardEnterpriseIncluded per Author10 GB10 GBOverage cost$0.25/GB/month$0.38/GB/monthMax dataset size25M rows / 25 GB1B rows / 1 TBMax on-demand refreshes32 per 24h32 per 24hCapacity allocationPer AWS RegionPer AWS RegionData Source ConnectivitySource TypeConnection MethodVPC RequiredAmazon S3Native connectorNoAmazon AthenaNative connectorNoAmazon RDSNative connectorYes (private)Amazon RedshiftNative connectorYes (private)On-premises databasesJDBC/ODBC via Direct ConnectYesThird-party SaaSNative or JDBC/ODBC connectorsVariesKey Questions to DocumentWhat is the acceptable data latency for each dashboard use case?What is the projected SPICE capacity requirement based on dataset sizes and growth rates?Which data sources require VPC connectivity or Direct Connect?How will SPICE refresh schedules be managed and monitored?What is the region selection strategy considering data residency requirements and SPICE capacity allocation?Which datasets will use SPICE vs. direct query, and what is the justification for each?LinksRelated: ADR-001 Edition SelectionRelated: ADR-004 Security and GovernanceReference: SPICE Best PracticesReference: SPICE DocumentationWAF Pillar MappingPerformance Efficiency: SPICE vs. direct query trade-off directly impacts query response timesCost Optimization: SPICE reduces per-query costs but requires capacity investmentReliability: SPICE provides independence from source system availabilitySustainability: Right-sizing SPICE capacity minimizes resource waste