# ADR-006: Embedding Multitenancy

**Status:** Pending
**Export Date:** 2026-09-08T13:54:28.765Z

---

## Original ADR Document

ADR-006: Embedding, Multi-Tenancy, and Namespace StrategyStatusProposedDate2026-09-08Decision MakersEnterprise ArchitectProduct Engineering LeadSecurity ArchitectPlatform Engineering LeadContext and Problem StatementOrganizations embedding QuickSight analytics into their own applications must decide between two embedding modes and design their multi-tenancy isolation model. These decisions are tightly coupled with the authentication model (ADR-002) and security controls (ADR-004). Custom namespaces — the strongest tenant isolation boundary — are only accessible to IAM Federated SSO users.Decision DriversEmbedding target: customer-facing applications, internal portals, or bothMulti-tenancy isolation requirements (namespace vs. RLS-based)Expected session volume and pricing model preferenceNumber of embedding domains to allow-list (max 100)User provisioning complexity toleranceSub-Decisions6a. Embedding ModeConsidered OptionsRegistered User Embedding (GenerateEmbedUrlForRegisteredUser) — Requires user provisioning in QuickSightAnonymous User Embedding (GenerateEmbedUrlForAnonymousUser) — No user provisioning required, uses tag-based RLSConsequencesRegistered: richer functionality (full console, dashboard, visual embedding), but requires maintaining user provisioning pipelinesAnonymous: simpler user management, but limited personalization and requires tag-based RLS for all data security6b. Multi-Tenancy Isolation ModelConsidered OptionsNamespace isolation — Logical containers isolating users and groups per tenant (Enterprise only, IAM Federation required)RLS-based isolation — Row-level security rules filter data per tenant within shared namespaceFolder-based access control — Organize assets in folders with per-tenant permissionsConsequencesNamespace: strongest isolation boundary, but constrains identity model to IAM federationRLS-based: works with any identity model, but requires careful rule managementFolder-based: organizational convenience, but not a true security boundary6c. Capacity Pricing for Embedded AnalyticsPricing ModelCostBest ForPer-session (pay-as-you-go)$0.30/session (30-min)Low/unpredictable volumeCapacity pricing$250/mo for 500 sessions + $0.50/additionalHigh-volume embedded deploymentsPer-user (Reader)$3/mo per readerKnown, stable user baseKey Questions to DocumentWill QuickSight be embedded in customer-facing applications, internal portals, or both?What is the expected session volume for capacity pricing planning?Is namespace-level tenant isolation required, or is RLS-based isolation sufficient?How many domains need to be allow-listed for embedding (max 100)?Should registered or anonymous embedding be used, and what are the security implications?How will tenant onboarding/offboarding be automated?LinksRelated: ADR-001 Edition Selection (Enterprise required for embedding)Related: ADR-002 Authentication and Identity (Namespace isolation requires IAM Federation)Related: ADR-004 Security and Governance (RLS/tag-based security for embedded users)Reference: Embedded Analytics APIReference: Namespaces DocumentationReference: Tag-based RLSWAF Pillar MappingSecurity: Tenant isolation model directly impacts data security postureCost Optimization: Embedding pricing model significantly affects total costReliability: Namespace isolation provides stronger fault isolation between tenantsPerformance Efficiency: Session-based pricing incentivizes efficient dashboard design