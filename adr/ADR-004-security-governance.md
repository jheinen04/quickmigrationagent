# ADR-004: Security Governance

**Status:** Pending
**Export Date:** 2026-09-08T13:54:28.765Z

---

## Original ADR Document

ADR-004: Security, Governance, and ComplianceStatusProposedDate2026-09-08Decision MakersSecurity ArchitectCompliance OfficerData Protection OfficerEnterprise ArchitectContext and Problem StatementQuickSight's security architecture spans data-level access controls, encryption, network isolation, audit logging, and compliance certifications. Enterprise edition is required for the full security stack. Organizations must decide on row-level security (RLS) approach, column-level security (CLS) strategy, encryption key management, network access controls, and compliance framework alignment.Decision DriversRegulatory and compliance requirements (HIPAA, PCI DSS, SOC, FedRAMP, ISO)Data classification and sensitivity levelsMulti-tenancy data isolation requirementsAudit and traceability requirementsNetwork security posture (IP restrictions, VPC endpoints)Sub-Decisions4a. Row-Level Security (RLS) ApproachConsidered OptionsUser-based RLS — Permissions dataset with UserName/GroupName columns filtering rows per registered user or groupTag-based RLS — Tags passed at runtime via embedding API for anonymous users; AND logic within a tile, OR logic across tilesCombined approach — User-based for registered users, tag-based for anonymous embedded usersConsequencesUser-based RLS requires maintaining a permissions dataset synchronized with user/group changesTag-based RLS is designed for anonymous embedded dashboard users onlyCombined approach provides maximum flexibility but increases management complexity4b. Column-Level Security (CLS) StrategyCLS restricts access to specific dataset columns per user or groupEach dataset can have multiple CLS rulesA user must be explicitly added to a rule for a restricted column to see itVisuals using restricted columns are hidden from unauthorized users4c. Encryption Key ManagementConsidered OptionsAWS-managed keys — Default encryption, no additional management overheadCustomer-managed KMS keys (CMK) — Full control over key lifecycle, all data access logged in CloudTrailConsequencesAWS-managed keys: simpler operations, less audit granularityCMK: full audit trail in CloudTrail for when and where data was accessed, but requires KMS key management4d. Network Access ControlsConsidered OptionsNo IP restrictions — Open access from any networkIP-based access restrictions — Limit account access to predefined IP rangesVPC endpoint policies — Restrict access from specific VPC endpointsCompliance CertificationsQuickSight is assessed under the following compliance programs:FedRAMPHIPAAPCI DSSSOCISO 9001, 27001, 27018, 27019Key Questions to DocumentWhich datasets require RLS, and should the organization use user-based rules, tag-based rules, or both?Which columns contain sensitive data requiring CLS restrictions?Should the organization use AWS-managed or customer-managed KMS keys for encryption at rest?What IP ranges should be allowed for QuickSight access?Which compliance frameworks must the QuickSight deployment satisfy?How will security configurations be audited and reviewed on an ongoing basis?What is the data classification scheme, and how does it map to RLS/CLS rules?LinksRelated: ADR-001 Edition SelectionRelated: ADR-002 Authentication and IdentityRelated: ADR-006 Embedding and Multi-TenancyReference: QuickSight Security Best PracticesReference: Row-Level SecurityReference: Column-Level SecurityReference: Customer-Managed KeysReference: Compliance ValidationWAF Pillar MappingSecurity: Primary pillar — all sub-decisions directly impact security postureOperational Excellence: Audit logging and compliance monitoring require operational processesCost Optimization: CMK adds KMS costs; RLS/CLS complexity affects maintenance effort